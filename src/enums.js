const profileTypes = {
	client: 'client',
	contractor: 'contractor',
};

const contractStatuses = {
	new: 'new',
	inProgress: 'in_progress',
	terminated: 'terminated',
};

module.exports = {
	profileTypes,
	contractStatuses,
};
